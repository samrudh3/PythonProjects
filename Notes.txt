Print Statement and its parameter 
object(s): Any object, and as many as you like. Will be converted to string before printed
sep='separator': Specify how to separate the objects, if there is more than one. Default is ' '
end='end': Specify what to print at the end. Default is '\n' (line feed)
file: An object with a write method. Default is sys.stdout
Parameters 2 to 4 are optional

Data Type 
Number = int float complex 
Text = str
Boolean = True and False
Sequenced Data = List and tuple 
Mapped Data = Dict 

In Python is Everything is objects

Operators 
+, -, %, /, *, // = Floor Division (17//5 = 3) 
** Exponential 

Type Casting = Conversion of one data type into other,Ex:int(a)
    Explicit 
    Implicit 

Strings are Immutable 
using ' ' or " " or """ """ anything works 
String methods wont change the value of string they will return a new string 

Conditional Statements
if, if else, if elif else 

Match Case : Switch Case in other language (Added in 3.10 and newer)
syntax :
match x:
    case 0:
    case 4:
    case _:(Default)

Loops : for,while    
for is used in range 
while is used when we know the condition, when condition is false, interpreter comes out of the loop, we can use else with while loop  

Break will be used to come out of the loop 
continue is used when we have skip a itration in the loop

Functions= block of code performs specific task
Argumennts : Default Args, keyword args, Variable len args, Required args

List - collections of any data type
List Comprehension
m = l (if m is effected l will also effect)

k = l + m (if m and l are effected, k will not get effected, concatinates the list)
Syntax : [Expresion(item) for item in range() if(Condition)]

Tuple = Immutable

F string from 3.6 Version

Doc String : are the string literals that appear right after the defination of method, class,modules
''' ''' = > It should be the first line after function body => otherwise the doc sting will be NONE

On import this = > Zen of Python will get print on the terminal

Recursion : is a process of defining something in terms of itself

Sets : Unordered collections of data items, do not contain duplicate elements 
Methods : s1.union(s2), s1.update(s2),  s1.intersection(s2), s1.intersection_update(s2), symmetric_difference, symmetric_difference_update
difference, difference_update
del s1 => deletes the set

Dictionary : Dic are ordered after 3.7 . Dict items are in key value pairs. separated by comma, enclosed with the {}

for and while loop with else

Exception Handling : process of handling the unwanted and unexpected events

We can throw the custom Errors using the keyword raise

Short Hand : if else can be Writte in 1 line 

Enumerate : is a built in function that allows to loop through the Sequence of elements, along with the index and value  

Virtual Environment : python -m venv <NAME>
tool used to isolate the specific environment on single machine, allowing us to work in different project, without any conflicts 
To activate the virtual environment : source <NAME>\Scripts\Activate.ps1(Powershell), Activate.bat(CMD)
To Deactivate just enter : deactivate

pip freeze => is CMD will give all the packages intalled 
pip freeze > requirement.txt
if above file is given to someone, he can run pip install -r requirement.txt 

import => is loading the code from python module into the current script 

if __name__ == "__main__" = idiom or common pattern to determine whether the script is being called directly or being imported
even just importing the module can execute the methods, if condition is not added

OS Module => Can be used to perform the opertaion in the bulk, to rename, create, delete etc
We can also use it for searching

Local variable and Global Variable: Variable are the named location in memory that stores values 

Files IO : read(r) = open in readonly mode, if not found throws error. r mode =is default 
write(w) = open in write mode, creates file fi doesn't exists
append(a) = add the content to the file
create(x) = this creates a files gives error if already exists
text(t) = 'rt' normal we are just defining as textfiles
binary(b) = 'rb' reads binary files like jpg,png,pdf

Using 'with' keyword not need to close the file
read() = reads the entire file in single string -> return str
readlines() = reads line by line -> return list
write() = writes single string-> takes only str # Add \n
writelines() = writes iterable like list # Add \n 

File Handlings(seek() and tell()) => used to work with a file objects and their position witin a file
These functions are part of built-in module
seek() => is a function alows to move the current position within file to a specific point, position is specified in bytes 
tell() => returns the cuurent postion within a file in bytes 
truncate() => if we want to truncate the file, f.truncate(5)/ Writes/Appends only 5 bytes 

Lambda Functions is a small anonymous function without name. it is written using lambda keyword
Syntax : lambda args : expression
we can pass the lambda function as argument for another function